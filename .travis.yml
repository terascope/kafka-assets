language: node_js
node_js:
  - "8"
jobs:
  include:
    - stage: GitHub Build Release
      if: tag IS present AND branch = master AND type != pull_request
      script: yarn build
      deploy:
        provider: releases
        api_key: $GITHUB_TOKEN
        file_glob: true
        file: builds/*.zip
        skip_cleanup: true