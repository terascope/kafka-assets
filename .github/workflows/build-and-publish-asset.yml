name: Build and Publish Teraslice Asset
run-name: ${{ github.actor }} is building and publishing the Teraslice Asset
on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  call-asset-build:
    uses: terascope/workflows/.github/workflows/asset-build-and-publish.yml@3905b55cb7879d996a044da64c637fe033f63619
    secrets: inherit
